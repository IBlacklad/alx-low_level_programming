#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Last Digit
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;


	for (c = 'a'; c <= 'z'; c++)
	{
		if (c != 'q' && c != 'e')
		{
			putchar(c);

		/*putchar('\n');*/
		} else {  }
	}
	return (0);


}
