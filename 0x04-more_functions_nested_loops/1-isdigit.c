#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include <main.h>
/**
 * _isdigit - checks if its a digit
 * @c: character to check
 * Return: 1 if its a digite therwise 0
 */

int _isdigit(int c)
{

	if (c >= '0' && c <= '9')
		return (1);
return (0);

}
