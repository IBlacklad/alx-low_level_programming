#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Postive or Negative
 * Return: Always 0 (success)
 */

int_islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{

		return(1);

	}
	else
		return (0);
}
